// Bot class
module.exports = function(name, botId, groupId) {
    this.name = name;
    this.botId = botId;
    this.groupId = groupId;
}

module.exports.prototype.say(msg, img) {
    if (img == undefined) {

    } else {

    }
}
